<?php

/*
 * função __autoload()
* carrega uma classe quando ela é necessária,
* ou seja, quando ela é instancia pela primeira vez. */
function __autoload($classe){
	$pastas = array('app/bo', 'app/config', 'app/controllers', 'app/view', 'app/vo');
	
	//Percorrer as pastas atrás de classes
	foreach($pastas as $pasta){
		if(file_exists("{$pasta}/{$classe}.class.php"))
			include_once "{$pasta}/{$classe}.class.php";
	}//fim do foreach
}//fim da autoload

/** Classe responsável por criar uma faxada para o cliente **/
class ClienteFacade {
	
	/** Método responsável por testar o retorno **/
	public function testeRetorno(){
		
	}//fim do método
	
}//fim da classe facade3

/** Parte de configuração do servidor **/
$server = new SoapServer(NULL, array('encoding' => 'ISO-8859-1', 'uri' => 'http://fga.unb.br/WsFGA-AppMobile/app/servidor.php'));
//define a classe que irá responder às chamadas remotas
$server->setClass('ClienteFacade');
//prepara-se para receber as chamadas remotas
$server->handle();

?>
